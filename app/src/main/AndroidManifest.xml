<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diaryofsecrets">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:name=".MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DefaultTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/diaryOfSecrets"
            android:screenOrientation="locked"
            android:theme="@android:style/Theme.Black.NoTitleBar"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CatalogActivity" />
        <activity
            android:name=".EditorActivity"
            android:parentActivityName=".CatalogActivity" />

        <provider
            android:name=".data.MessageProvider"
            android:authorities="com.example.diaryofsecrets"
            android:exported="false" />

        <receiver android:name=".navigation.AlarmReceiver" />
        <receiver
            android:name=".navigation.AlarmBootReceiver"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity android:name=".AppLockScreenActivity"></activity>
    </application>

</manifest>